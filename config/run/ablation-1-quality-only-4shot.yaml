# Ablation: rationale-quality-only (no schema/structure scoring) 4-shot

run_id: ablation-1-quality-only-4shot

method:
  name: rationale-quality-only
  description: "Rationale quality scoring only (no schema adaptation)"
  shot_budget: 4
  use_schema_scoring: false
  use_structure_scoring: false
  use_brevity_scoring: true
  use_answer_consistency: true
  
inference:
  dataset: gsm8k
  model: gemini-2.5-flash
